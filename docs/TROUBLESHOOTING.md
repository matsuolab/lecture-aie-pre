# 困ったときの確認（確認中）

動画を止めて、いま表示されているアカウント名・リポジトリ名・ファイル名が、動画で操作している場所に対応しているか確認してください。

Codespaceでは、ターミナルで次を実行すると現在のブランチ名と変更したファイルを確認できます。

```bash
git status
```

表示されたブランチ名とファイル名を、止めた区間と照らし合わせてください。合わない場合は、別の名前へ切り替えたりファイルを削除したりせず、その演習を終了してください。
